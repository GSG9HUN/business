package com.dc.melodiasmario.core.auth.data.local

import kotlin.test.Test

// TODO: Implement Android host tests for the current SecureAuthSessionStorageImpl behavior.
class SecureAuthSessionStorageImplTest {

    @Test
    fun getSessionReturnsNullBeforeSessionIsSaved() {
        // TODO: Given fresh storage, when getSession is called, then it returns null.
    }

    @Test
    fun saveSessionStoresSessionInMemory() {
        // TODO: Given a session, when saveSession is called, then getSession returns the same session.
    }

    @Test
    fun saveSessionOverwritesExistingSession() {
        // TODO: Given existing session, when another session is saved, then getSession returns the latest session.
    }

    @Test
    fun clearSessionRemovesStoredSession() {
        // TODO: Given stored session, when clearSession is called, then getSession returns null.
    }
}
