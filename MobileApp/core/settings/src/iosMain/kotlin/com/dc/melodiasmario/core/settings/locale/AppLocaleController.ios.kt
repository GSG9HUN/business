package com.dc.melodiasmario.core.settings.locale

actual class AppLocaleController {
    actual fun applyLocale(languageCode: String) {
        // iOS app language handling needs native app-level integration.
    }
}
